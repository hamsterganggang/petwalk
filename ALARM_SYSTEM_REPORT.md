# PetWalk 실시간 알림 시스템 구축 및 데이터 구조 최적화 보고서

**작업 일자:** 2026년 1월 10일  
**주요 작업 요약:** Firebase Cloud Functions v2 도입, FCM 실시간 알림 연동, 사용자 데이터 컬렉션 통합 및 인증 로직 수정.

---

## 1. Firebase Cloud Functions v2 환경 구축 및 배포
- **배포 환경 최적화:** Cloud Functions v2를 도입하여 성능 및 확장성을 확보하였습니다.
- **리전 설정:** Firestore 데이터베이스 위치에 맞춰 함수 배포 지역을 **서울(`asia-northeast3`)**로 설정하여 네트워크 지연 시간을 단축하였습니다.
- **권한 문제 해결:** 2세대 함수 배포 시 발생하는 Eventarc 서비스 에이전트의 권한(Permission Denied) 문제를 Google Cloud IAM 설정을 통해 해결하였습니다.
- **트리거 리전 명시:** Firestore와 Cloud Functions 간의 리전 불일치 방지를 위해 `onDocumentCreated` 옵션에 명시적으로 리전을 고정하여 트리거 안정성을 확보하였습니다.

## 2. 실시간 알림(FCM) 트리거 로직 구현
- **좋아요(Like) 알림:** `likes` 컬렉션에 새 문서 생성 시, 해당 산책 기록(`walks`)의 작성자를 찾아 실시간 푸시 알림을 발송하는 로직을 구현하였습니다.
- **팔로우(Follow) 알림:** `follows` 컬렉션에 팔로우 관계가 생성되면 피팔로워(Following)에게 실시간 알림을 전송하는 로직을 구현하였습니다.
- **예외 처리:** 본인이 본인 글에 좋아요를 누르거나 본인을 팔로우하는 경우 알림이 가지 않도록 필터링 로직을 추가하였습니다.
- **서버 로그 강화:** 알림 전송 과정을 단계별로 `console.log`를 남겨 장애 발생 시 즉각적인 원인 파악이 가능하도록 디버깅 코드를 삽입하였습니다.

## 3. 사용자 데이터 구조 통합 (`users` → `profiles`)
- **컬렉션 단일화:** 앱 내 `ProfileDataService`와 인증 서비스 간의 데이터 불일치를 해결하기 위해 기존 `users` 컬렉션의 정보를 **`profiles` 컬렉션으로 통합**하였습니다.
- **데이터 스키마 정렬:** `UserProfile` 모델에 맞춰 필드명(예: `photoURL` → `photoUrl`, `followerCount` → `followers`)을 일관성 있게 수정하였습니다.
- **인증 로직 수정:** `UserAuthenticationService` 내의 모든 CRUD 로직이 `profiles` 컬렉션을 바라보도록 전면 수정하여 데이터 무결성을 확보하였습니다.

## 4. Flutter 클라이언트 FCM 연동 및 인증 핸들러 개선
- **FCM 토큰 관리:** `NotificationService`를 구현하여 앱 실행 시 기기별 FCM 토큰을 생성하고, Firestore의 `profiles` 문서에 자동으로 저장/업데이트하도록 구현하였습니다.
- **인증 흐름 최적화:**
  - **이메일 로그인/회원가입:** 인증 성공 즉시 FCM 토큰을 업데이트하는 로직을 추가하였습니다.
  - **구글 로그인:** 구글 인증 직후 사용자 프로필 생성과 동시에 FCM 토큰을 동기화하도록 `GoogleSignInHandler`를 수정하였습니다.
- **빌드 에러 해결:** 리팩토링 과정에서 누락된 `checkAuthStatus`, `hasNickname` 등의 필수 메서드를 복구하고 최신 데이터 구조에 맞게 재설계하여 컴파일 에러를 해결하였습니다.

## 5. 현재 상태 및 기대 효과
- **현재 상태:** Cloud Functions 배포 완료 및 정상 작동 중이며, 로그인 시 각 계정별 FCM 토큰이 `profiles` 컬렉션에 정상 등록됨을 확인하였습니다.
- **기대 효과:** 사용자 간 상호작용 발생 시 즉각적인 피드백을 제공하여 앱 리텐션을 향상시키고, 정리된 데이터 구조를 통해 향후 기능 확장 시 개발 속도를 단축할 수 있습니다.
